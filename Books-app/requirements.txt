# 서점 APP 의존성
#
# [버전 표기 방침]
#   지금은 범위로 둔다. 8단계 CI 에서 lock 파일로 고정한다
#   → 범위로 두면 빌드할 때마다 다른 버전이 들어올 수 있다
#   → "어제는 됐는데 오늘 안 된다" 의 흔한 원인이다
#   → 그래서 CI 에서는 반드시 고정한다

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# psycopg3 를 쓴다. psycopg2 가 아니다
#   3 은 비동기(async)와 커넥션 풀을 기본 제공한다
#   [binary] 미리 컴파일된 배포판. gcc 가 필요 없다 (07 문서)
#   [pool]   커넥션 풀. 05 문서의 db_pool_* 지표를 여기서 얻는다
psycopg[binary,pool]>=3.2,<4.0

redis>=5.0,<6.0
prometheus-client>=0.20,<1.0
